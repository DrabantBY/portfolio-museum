import './styles.scss';

const MenuButton = () => {
    return (
      <button type='button' className='menu-btn'>
        <span />
        <span />
        <span />
      </button>
    );
  };
  
  export default MenuButton;
  
